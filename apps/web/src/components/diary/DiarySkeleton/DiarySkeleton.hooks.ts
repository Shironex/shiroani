import type { IDiarySkeletonView } from './DiarySkeleton.types';

/** Thin view: the skeleton is purely presentational, nothing to derive. */
export function useDiarySkeleton(): IDiarySkeletonView {
  return {};
}
