import type { AniListActivity } from '@shiroani/shared';

export interface ISocialActivityRowProps {
  item: AniListActivity;
}

export interface ISocialActivityRowView {
  readonly relative: string;
}
