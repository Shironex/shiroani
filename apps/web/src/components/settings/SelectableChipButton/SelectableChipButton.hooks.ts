import type { ISelectableChipButtonView } from './SelectableChipButton.types';

/**
 * The selectable chip is presentational. The factory exists to satisfy the
 * component-folder convention and exposes no view-model.
 */
export function useSelectableChipButton(): ISelectableChipButtonView {
  return {};
}
