import type { IAnimeInfoPeopleView } from './AnimeInfoPeople.types';

/** AnimeInfoPeople is purely presentational — nothing to derive. */
export function useAnimeInfoPeople(): IAnimeInfoPeopleView {
  return {};
}
