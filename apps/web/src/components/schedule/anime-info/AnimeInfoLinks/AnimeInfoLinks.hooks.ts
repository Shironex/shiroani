import type { IAnimeInfoLinksView } from './AnimeInfoLinks.types';

/** AnimeInfoLinks is presentational — nothing to derive. */
export function useAnimeInfoLinks(): IAnimeInfoLinksView {
  return {};
}
