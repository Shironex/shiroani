import type { ISectionLabelView } from './SectionLabel.types';

/** SectionLabel is purely presentational — nothing to derive. */
export function useSectionLabel(): ISectionLabelView {
  return {};
}
