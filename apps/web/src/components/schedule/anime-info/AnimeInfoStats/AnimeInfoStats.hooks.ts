import type { IAnimeInfoStatsView } from './AnimeInfoStats.types';

/** AnimeInfoStats derives nothing — i18n lives inline in the shell. */
export function useAnimeInfoStats(): IAnimeInfoStatsView {
  return {};
}
