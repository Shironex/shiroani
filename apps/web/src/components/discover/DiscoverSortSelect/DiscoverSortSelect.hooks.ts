import type { IDiscoverSortSelectView } from './DiscoverSortSelect.types';

/** Thin view: the select is fully driven by props + i18n, nothing to derive. */
export function useDiscoverSortSelect(): IDiscoverSortSelectView {
  return {};
}
