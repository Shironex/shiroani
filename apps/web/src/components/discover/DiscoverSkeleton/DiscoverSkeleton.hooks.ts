import type { IDiscoverSkeletonView } from './DiscoverSkeleton.types';

/** Thin view: the skeleton is purely presentational, nothing to derive. */
export function useDiscoverSkeleton(): IDiscoverSkeletonView {
  return {};
}
