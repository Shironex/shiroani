import type { IRandomSkeletonView } from './RandomSkeleton.types';

/** Thin view: the showcase skeleton is purely presentational. */
export function useRandomSkeleton(): IRandomSkeletonView {
  return {};
}
