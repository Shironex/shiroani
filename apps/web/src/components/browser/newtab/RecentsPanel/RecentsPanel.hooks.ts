import type { IRecentsPanelView } from './RecentsPanel.types';

/** RecentsPanel is presentational — all data arrives via props. */
export function useRecentsPanel(): IRecentsPanelView {
  return {};
}
