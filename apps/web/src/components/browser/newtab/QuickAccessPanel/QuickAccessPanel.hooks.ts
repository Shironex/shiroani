import type { IQuickAccessPanelView } from './QuickAccessPanel.types';

/** QuickAccessPanel is presentational — all data arrives via props. */
export function useQuickAccessPanel(): IQuickAccessPanelView {
  return {};
}
