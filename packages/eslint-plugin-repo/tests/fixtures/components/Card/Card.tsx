// Existence-only fixture: index-must-reexport-default activates for a folder's
// index.ts only when a sibling `<Folder>.tsx` exists on disk. This file is that
// sibling for the Card/ folder; its contents are irrelevant to the rule.
export default {};
